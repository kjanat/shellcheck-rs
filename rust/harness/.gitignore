# Derived caches / reports — regenerated by run_conformance.py, never committed.
goldens.jsonl
port.jsonl
coverage.json
run.log
extract.log
__pycache__/

# baseline.json is intentionally NOT ignored: it is the committed conformance
# gate baseline that `run_conformance.py --gate` checks against.
